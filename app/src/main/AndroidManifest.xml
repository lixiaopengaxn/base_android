<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.xp.develop">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>


    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon"
        android:supportsRtl="true"
        tools:replace="android:icon"
        android:theme="@style/AppTheme">
        <!--//适配刘海-->
        <meta-data android:name="android.notch_support" android:value="true"/>

        <!-- 如果您项目中的所有页面都只需要以高或宽中的一个作为基准进行适配的话, 那就只需要填写高或宽中的一个设计图尺寸即可 -->
        <!--<meta-data-->
            <!--android:name="design_width_in_dp"-->
            <!--android:value="400"/>-->
        <!--<meta-data-->
            <!--android:name="design_height_in_dp"-->
            <!--android:value="1960"/>-->


        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.xp.develop.fileprovider"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>



        <activity android:name=".welcome.WelcomeActivity" android:theme="@style/WelcomeTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".activity.MainActivity"
            android:screenOrientation="portrait"/>



        <!--test-->


        <activity android:name=".test.activity.TestActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".test.activity.JinPingMeiActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".test.activity.TestWebViewActivity"
            android:screenOrientation="portrait"
            android:launchMode="singleTask"/>

        <activity android:name=".test.activity.TestPingFenActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".test.activity.TestTaskPhotoActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".test.activity.TestScrollActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".test.activity.NewActivity"
            android:screenOrientation="portrait"/>

        <!--test-->


    </application>

</manifest>